export function UsersPage() {
  return (
    <section className="rounded-[28px] border border-border bg-card p-8 shadow-sm">
      <h2 className="text-[clamp(2rem,3vw,3rem)] font-semibold leading-none tracking-[-0.04em] text-card-foreground">
        Users
      </h2>
    </section>
  );
}
